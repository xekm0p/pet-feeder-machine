
server:
  port: 8081
  ssl:
    enabled: true
    key-store: src/main/resources/bootsecurity.p12
    key-store-password: ssasha
    key-store-type: PKCS12
    kkey-alias: bootsecurity

spring:
  datasource:
    url: jdbc:mysql://localhost:3308/pet_feeder?useSSL=false
    driver-class-name: com.mysql.jdbc.Driver
    username: admin
    password: admin

#server:
#  ssl:
#    key-alias: https-example
#    key-store-type: JKS
#    key-password: password
#    key-store: classpath:https-exaple.jks

  liquibase:
    change-log: classpath:db/master.xml

solace:
  java:
    host: tcp://localhost:55555
    msgVpn: default
    clientUsername: default
    clientPassword: default

